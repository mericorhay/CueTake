// StudioFeature
//
// Camera preview + teleprompter overlay + recording + segment retake. One screen, two modes: whole script or a single segment.
//
// Depends on: Domain, DesignSystem, Teleprompter, CaptureEngine, SpeechEngine.
// Never imports another feature; the app target routes between features.
